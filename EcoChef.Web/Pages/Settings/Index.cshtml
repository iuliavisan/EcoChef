@page
@model EcoChef.Web.Pages.Settings.IndexModel

@{
    ViewData["Title"] = "Setări";
}

<h1>Setări admin</h1>

<p>
    <a asp-page="Create">Creează</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Setari[0].MarjaProfit)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Setari) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MarjaProfit)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Editează</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Detalii</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Șterge</a>
            </td>
        </tr>
}
    </tbody>
</table>
